const colors = {
  accent: '#000CFB',
  font: '#2E2E2E',
}

const fontSizes = {
  medium: '1.6rem',
  extraLarge: '4rem',
}

const fontWeight = {
  thin: 100,
  light: 300,
  regular: 400,
  bold: 700,
  black: 900,
}

const breakpoints = {
  sm: '576px',
  md: '768px',
  lg: '992px',
  xl: '1200px',
  xxl: '1400px',
}

const theme = {
  colors,
  fontSizes,
  fontWeight,
  breakpoints,
}

export default theme
