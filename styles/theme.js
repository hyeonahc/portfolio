const colors = {
  accent: '#000CFB',
  black: '#2E2E2E',
  white: '#FFFFFF',
}

const fontFamily = {
  lato: 'Lato',
  poppins: 'Poppins',
}

const fontSizes = {
  md: '1.6rem',
  xmd: '1.8rem',
  lg: '2rem',
  xxl: '5rem',
}

const fontWeight = {
  thin: 100,
  light: 300,
  regular: 400,
  semibold: 600,
  bold: 700,
  black: 900,
}

const breakpoints = {
  sm: '576px',
  md: '768px',
  lg: '992px',
  xl: '1200px',
  xxl: '1400px',
  xxxl: '1600px',
}

const theme = {
  colors,
  fontFamily,
  fontSizes,
  fontWeight,
  breakpoints,
}

export default theme
